{{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.author) }}
    {{ form_row(form.yearOfPublication) }}
    {{ form_row(form.isbn) }}
    <input type="hidden" name="_token" value="{{ csrf_token('book_form') }}">
    <button type="submit" class="btn btn-primary">Save</button>
{{ form_end(form) }}
